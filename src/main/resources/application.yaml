#spring:
#    application:
#        name: scanny
#    datasource:
#        url: jdbc:postgresql://localhost:5433/scanny
#        username: postgres
#        password: bazepodataka
#    jpa:
#        hibernate:
#            ddl-auto: update
#        properties:
#            hibernate:
#                dialect: org.hibernate.dialect.PostgreSQLDialect
#word-translations:
#    path: data/word_translations.json
spring:
    application:
        name: scanny
    datasource:
        url: ${SPRING_DATASOURCE_URL}
        username: ${SPRING_DATASOURCE_USERNAME}
        password: ${SPRING_DATASOURCE_PASSWORD}
        driver-class-name: org.postgresql.Driver
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect
        open-in-view: false
word-translations:
    path: data/word_translations.json
server:
    port: ${PORT:8080}

