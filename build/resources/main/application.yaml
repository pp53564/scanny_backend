spring:
    application:
        name: scanny
    datasource:
        url: jdbc:postgresql://localhost:5433/scanny
        username: postgres
        password: bazepodataka
    jpa:
        hibernate:
            ddl-auto: update
        properties:
            hibernate:
                dialect: org.hibernate.dialect.PostgreSQLDialect